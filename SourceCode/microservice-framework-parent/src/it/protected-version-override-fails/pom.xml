<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Contract #9(a): Protected version override must be caught by Enforcer.
         Strategy: Override the protected spring-boot.version property from the
         Framework Parent. The enforce-protected-versions rule requires
         spring-boot.version to match exactly 3.3.13, so any override will
         cause Enforcer to fail with a clear diagnostic message. -->
    <parent>
        <groupId>com.microservice.framework</groupId>
        <artifactId>microservice-framework-starter-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <artifactId>protected-version-override-fails-fixture</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <!-- Override the protected version property managed by Framework Parent -->
    <properties>
        <spring-boot.version>3.2.0</spring-boot.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
    </dependencies>
</project>
